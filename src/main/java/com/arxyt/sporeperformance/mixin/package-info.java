/**
 * Narrow Mixins into verified Spore internals. Optional targets are selected by the config plugin;
 * every injection also has a runtime guard so a signature drift degrades to original behaviour.
 */
package com.arxyt.sporeperformance.mixin;
